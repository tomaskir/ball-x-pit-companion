import { defineConfig } from 'astro/config';

// Static output deployed to GitHub Pages at https://tomaskir.github.io/ball-x-pit-companion/
export default defineConfig({
  site: 'https://tomaskir.github.io',
  base: '/ball-x-pit-companion',
});
